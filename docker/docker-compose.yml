version: "3"

services:
    app:
        build:
            context: ../
            dockerfile: ./docker/Dockerfile.${MODE}
        volumes:
            - ..:/app
        ports:
            - ${PORT}:${PORT}
