version: "3"

services:
    app:
        build:
            context: ../
            dockerfile: ./docker/Dockerfile.prod
        volumes:
            - ..:/app
        environment:
            GITHUB_USERNAME: ${GITHUB_USERNAME}
            GITHUB_TOKEN: ${GITHUB_TOKEN}
