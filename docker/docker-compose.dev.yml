version: "3"

services:
    app:
        build:
            context: ../
            dockerfile: ./docker/Dockerfile.dev
        command: yarn start
        volumes:
            - ..:/app
        ports:
            - ${PORT}:${PORT}
